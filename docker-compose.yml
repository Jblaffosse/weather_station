version: '3.8'

services:
  flask-app:
    build: .
    container_name: flask_weather_station
    ports:
      - "10500:10500"
    volumes:
      - .:/app
    environment:
      - FLASK_APP=weather_station.py
      - FLASK_ENV=production